/* General Styles */
* {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
}

body {
    font-family: 'Arial', sans-serif;
    background: linear-gradient(135deg, #b7a9d1, #9f85e1); /* Light Purple Gradient Background */
    color: #333;
    display: flex;
    justify-content: center;
    align-items: center;
    height: 100vh;
    margin: 0;
    padding: 20px;
}

.game-container {
    width: 100%;
    max-width: 800px;
    background-color: #fff;
    border-radius: 12px;
    padding: 20px;
    text-align: center;
    border: 5px solid #d6aaff; /* Light Purple Border */
    box-shadow: 0 4px 8px rgba(0, 0, 0, 0.2);
    position: relative;
}

/* Header */
header {
    margin-bottom: 20px;
    color: #4b6e2a;
}

h1 {
    font-size: 2.5em;
    color: #4b6e2a;
    margin-bottom: 10px;
}

p {
    font-size: 1.2em;
    color: #555;
}

/* NPC Dialogue */
.npc-dialogue {
    font-size: 1.2em;
    background-color: #f0f8ff;
    padding: 15px;
    border-radius: 8px;
    margin-bottom: 20px;
    color: #333;
}

/* Language Challenge Section */
.challenge {
    margin-bottom: 20px;
}

h2 {
    font-size: 1.8em;
    color: #333;
}

.italic {
    font-style: italic;
}

input[type="text"] {
    width: 80%;
    padding: 12px;
    font-size: 1.2em;
    margin-bottom: 15px;
    border-radius: 5px;
    border: 1px solid #ddd;
}

button {
    padding: 12px 20px;
    font-size: 1.2em;
    background-color: #28a745;
    color: white;
    border: none;
    border-radius: 5px;
    cursor: pointer;
    transition: background-color 0.3s ease;
}

button:hover {
    background-color: #218838;
}

/* Feedback Section */
.feedback {
    margin-top: 20px;
}

.feedback p {
    font-size: 1.2em;
}

.feedback.correct p {
    color: #5bc0de;
}

.feedback.incorrect p {
    color: #d9534f;
}

/* Popup Modal */
.popup {
    display: none;
    position: fixed;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    background: rgba(0, 0, 0, 0.7);
    justify-content: center;
    align-items: center;
    z-index: 1000;
}

.popup-content {
    background: #fff;
    padding: 30px;
    border-radius: 10px;
    text-align: center;
    max-width: 400px;
    width: 100%;
}

/* Fade-in animation for pop-up */
@keyframes fadeIn {
    0% {
        opacity: 0;
    }
    100% {
        opacity: 1;
    }
}

.popup button {
    padding: 10px 20px;
    font-size: 1.2em;
    background-color: #007bff;
    color: white;
    border: none;
    border-radius: 5px;
    cursor: pointer;
}

.popup button:hover {
    background-color: #0056b3;
}

@media (max-width: 600px) {
    .game-container {
        padding: 15px;
        width: 100%;
        max-width: 100%;
    }

    h1 {
        font-size: 2em;
    }

    p {
        font-size: 1em;
    }

    .challenge input[type="text"] {
        width: 100%;
    }

    button {
        width: 100%;
    }

    .btn-popup {
        width: 100%;
        padding: 15px;
    }

    .progress {
        margin-top: 20px;
    }
}
